<Page x:Class="Wpf_SmallWorld.PageJeu"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	  Title="PageJeu"
      Loaded="Window_Loaded" Background="#FFF3F3B5">

    <DockPanel Background="#FFF3F3B5">
        <!-- <Grid DockPanel.Dock="top" Height="200" Name="Carte" Background="BlanchedAlmond"/>  -->
        <Grid ShowGridLines="true" DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Column="0"  Name="infolabel" Content="fjhfdkdfhguio"/>
            <Button Grid.Column="1" Width="100" Height="40" HorizontalAlignment="Center" Content="Fin de tour"/>
            <Label Grid.Column="2"  Name="infolabel2" Content="fjhfdkdfhguio"/>
        </Grid>
        <Grid Name="Carte" Background="Azure" HorizontalAlignment="Center" VerticalAlignment="Center">
            <!-- Définition du rectangle matérialisant la selection -->
            <Rectangle Style="{StaticResource selectionStyle}" Name="selectionRectangle" />
            <!-- Chaque case de la grille contient potentiellement un rectangle de selection + une unité (qui ne sera pas sensible aux evts)-->
            <Ellipse Fill="White" Width="10" Height="10" Name="unitEllipse" Panel.ZIndex="444" IsHitTestVisible="False" />

        </Grid>
    </DockPanel>
</Page>
